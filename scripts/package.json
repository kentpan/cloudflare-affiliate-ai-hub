{
  "name": "affiliate-ai-hub-scripts",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "Standalone Node TypeScript backend — runs the AI picker and writes to ../.data/. Designed to run in GitHub Actions independently of the web app.",
  "scripts": {
    "start": "tsx src/index.ts",
    "seed": "tsx src/seed.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "date-fns": "^3.6.0",
    "z-ai-web-dev-sdk": "^0.0.18",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.16.11",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
